# Font Files

This directory contains system fonts used in the Battle Betz app.

The app uses system fonts to ensure compatibility across all platforms.

## Font Usage

Instead of custom fonts, we're using system fonts for better reliability and performance.